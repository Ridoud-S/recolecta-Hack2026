version: '3.8'

services:
  recolecta-backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILE: dev
      JWT_SECRET: miClaveSecretaSuperLargaParaQueNoTrueneJWT1234567890
    restart: unless-stopped
